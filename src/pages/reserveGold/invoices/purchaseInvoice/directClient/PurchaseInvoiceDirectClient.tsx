import React from "react";

const PurchaseInvoiceDirectClient = (props) => {
  const { stage, setStage } = props;
  return <div>PurchaseInvoiceDirectClient</div>;
};

export default PurchaseInvoiceDirectClient;
