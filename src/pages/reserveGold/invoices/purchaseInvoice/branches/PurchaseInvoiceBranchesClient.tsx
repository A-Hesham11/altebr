import React from "react";

const PurchaseInvoiceBranchesClient = (props) => {
  const { stage, setStage } = props;

  return <div>PurchaseInvoiceBranchesClient</div>;
};

export default PurchaseInvoiceBranchesClient;
